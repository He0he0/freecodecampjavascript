function palindrome(str) {
 
 let filter = /[a-z0-9]/gi;
  let newStr= str.match(filter); 
  let testStr = [];
  for (let i =newStr.length-1;i>=0;i--){
    testStr.push(newStr[i]);

  }
return newStr.join(" ").toLowerCase() === testStr.join(" ").toLowerCase();
  }
       
  palindrome("eye");
